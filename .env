# Base URL for the backend API used by the frontend service (src/services/api.ts)
# Vite exposes variables starting with VITE_ to the client-side code via import.meta.env
VITE_API_BASE_URL=http://localhost:3001/api